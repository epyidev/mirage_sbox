# Editor.GameMode

Registers a widget with the input system, so it uses SDL.

- **Kind:** static class
- **Namespace:** `Editor`
- **Assembly:** `Sandbox.Tools`

## Methods

### Static methods

- `static System.Void SetPlayWidget(Editor.SceneRenderingWidget widget)`
  - Given a widget, register it for SDL input, and tell the engine this is the swapchain we have
- `static System.Void ClearPlayMode()`
