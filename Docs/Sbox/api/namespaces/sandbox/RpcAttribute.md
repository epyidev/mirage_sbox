# Sandbox.RpcAttribute

Marks a method as being an RPC. This means that it can be called over the network.

- **Kind:** attribute
- **Namespace:** `Sandbox`
- **Assembly:** `Sandbox.Engine`
- **Base:** `System.Attribute`

## Properties

- `Sandbox.NetFlags Flags`
