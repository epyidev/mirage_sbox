# Sandbox.TextRendering

- **Kind:** static class
- **Namespace:** `Sandbox`
- **Assembly:** `Sandbox.Engine`

## Methods

### Static methods

- `static Sandbox.Texture GetOrCreateTexture(Sandbox.TextRendering.Scope scope, Vector2 clip, Sandbox.TextFlag flag)`
  - Create a texture from the scope. The texture will either be a cached version or will be rendered immediately
