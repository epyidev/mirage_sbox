# Sandbox.VR.VRModelRenderer.ModelSources

Represents a controller to use when fetching the model (which device)

- **Kind:** enum
- **Namespace:** `(global)`
- **Assembly:** `Sandbox.Engine`
- **Base:** `System.Enum`
- **Declaring type:** `Sandbox.VR.VRModelRenderer`

## Values

- `LeftHand` - The left controller
- `RightHand` - The right controller
