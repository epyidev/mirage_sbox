# Sandbox.Audio.AudioProcessor.ListenerState

One of these is created for every listener that uses an audio processor.

- **Kind:** abstract class
- **Namespace:** `(global)`
- **Assembly:** `Sandbox.Engine`
- **Declaring type:** `Sandbox.Audio.AudioProcessor`

## Constructors

- `ListenerState()`

## Methods

### Instance methods

- `virtual System.Void OnDestroy()`
  - Called when audio processor or the listener is removed.
